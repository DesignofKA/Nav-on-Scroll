Second-Nav
==========================

A quick solution to adding to add a **second navigation**

Default configuration:

```javascript
$(document).ready(function() {
    $('.scroll__section').secondNav();
});
```

**Note** This will *slidein* the second-nav out from the top once it is past its marker & *slideout* the second-nav upwards once back before its marker.
